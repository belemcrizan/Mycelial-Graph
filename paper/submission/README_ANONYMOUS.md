# Anonymous supplementary material (TMLR)

This ZIP supports double-blind review of Paper A.

**Headline result.** Frozen MG-EXP-V1 confirmatory experiment: primary hypothesis
`REFUTED`. At ρ=0.50 hierarchical node-edge pooling recovered 42.1% slower than
edge-only adaptation (95% CI +11.9% to +79.1%, n=97). The ρ=0 negative-transfer
safety gate also failed.

Do not treat this as independent reproduction. Reproduction here is internal and
automated.

## One-command verification

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e .
python reproduce_confirmatory.py
```

Linux-first. On Windows PowerShell use `.venv\Scripts\Activate.ps1`.

`python reproduce_confirmatory.py --full` re-executes 1940 trials (minutes).
Raw confirmatory traces are not in git; they are regenerated by `--full`.

Measured decision CPU of the sealed run: **0.122 CPU-hours**.

## Equalization triage

Class **EQ-B** (post-confirmatory diagnostic): disclose the effective-policy
mismatch, do not overwrite `REFUTED`.

## Identity

This package is anonymized. Do not search for an identified repository while
reviewing.
