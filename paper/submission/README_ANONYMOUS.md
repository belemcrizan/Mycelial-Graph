# Anonymous supplementary material (TMLR)

This ZIP supports double-blind review of Paper A.

**Headline result.** Frozen MG-EXP-V1 confirmatory experiment: primary hypothesis
`REFUTED`. At ρ=0.50 the fixed hierarchical routing update recovered 42.1% slower
than the edge-only comparator under the frozen V1 configuration (95% CI +11.9% to
+79.1%, n=97). The ρ=0 negative-transfer safety gate also failed.

This is a result for the complete frozen policy mechanism. Class **EQ-B**
(post-confirmatory diagnostic) means effective policy scale was not equalized, so
attribution to hierarchical representation alone is unsupported. Do not overwrite
`REFUTED`.

Do not treat this as independent reproduction. Reproduction here is internal and
automated.

## Verification versus full reproduction

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.lock.txt
python -m pip install -e .
python reproduce_confirmatory.py
```

`python reproduce_confirmatory.py` verifies sealed hashes and, if local raw trials
exist, recomputes the frozen contrast. It is not complete end-to-end reproduction.

`python reproduce_confirmatory.py --full` re-executes 1940 trials from frozen
inputs (minutes). Raw confirmatory traces are not in git; they are regenerated by
`--full`.

Linux-first. On Windows PowerShell use `.venv\Scripts\Activate.ps1`.

Canonical sealed-run decision CPU: **0.122 CPU-hours** (437.59375 CPU-s), from
per-trial `decision_cpu_seconds`. A later `--full` replay is a different metric
(see `research/runtime.json`).

## Identity

This package is anonymized. Do not search for an identified repository while
reviewing.
