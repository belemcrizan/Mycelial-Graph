# Replay

Layer B: offline execution on logged or public records.

Preserve timestamps, model/version strings, and do not rewrite messy failures into hierarchical shocks.

OPE requires `x_t, a_t, r_t, p_t(a_t|x_t)`. If propensity is missing, IPS is unsupported.
