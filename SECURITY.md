# Security

V1 is a local research simulator. It does not require API keys, cloud credentials, or production data.

Do not commit credentials, provider traces containing customer data, or sensitive task content. Future live-provider adapters must use environment-based secret management, data minimization, explicit spending limits, and a hard policy boundary outside the adaptive reward function.

For private vulnerability reports, contact the repository owner through a private GitHub security advisory when available.

