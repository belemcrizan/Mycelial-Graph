# Theory roadmap

Track B, isolated from V1.

1. Freeze the minimal model (done as text + toy generator).
2. Derive NT and a toy $\rho^*$ under Gaussian MSE (sketch only).
3. Attempt a genuine RRT-relevant bound or document impossibility.
4. Pre-register MG-EXP-V1.5-RHO only after a prediction exists that uses the same estimand as the experiment.
5. Structural uncertainty $P(G\mid\mathrm{data})$ and abstention: later.
6. Causal DAG / $do(\cdot)$: separate version, not implied by the execution graph.

Collaboration targets: online learning, bandit theory, statistics. Open problems are listed in `theory/CONJECTURES.md` and `theory/LOWER_BOUNDS.md`.
