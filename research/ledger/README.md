# Append-only ledger

This file is not a substitute for artifacts. Sealed experiment_ids cannot be duplicated.

Use:

```text
python -c "from mycelial_graph.science.ledger import append_entry; ..."
```

Initial rows are written after executable steps in this cycle.
